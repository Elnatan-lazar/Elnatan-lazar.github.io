import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Enable static export for GitHub Pages if needed; remove for Vercel with server actions
  // output: "export",
};

export default nextConfig;
